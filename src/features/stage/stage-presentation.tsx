"use client";
import { useEffect, useState } from "react";
import type { StageSnapshot } from "./stage-state";
import { formatStageTime, remainingSeconds } from "./stage-timing";
type Props = { block?: { title: string; durationSeconds: number }; snapshot: StageSnapshot | null; variant?: "preview" | "stage" };
export function StagePresentation({ block, snapshot, variant = "preview" }: Props) { const [now, setNow] = useState(0); useEffect(() => { const timer = setInterval(() => setNow(Date.now()), 1000); return () => clearInterval(timer); }, []); if (snapshot?.activeMessageContent && (!snapshot.messageExpiresAt || now < snapshot.messageExpiresAt)) return <div className={`stage-presentation stage-${variant}`}><strong>{snapshot.activeMessageContent}</strong><small>COMUNICADO</small></div>; if (!snapshot || !block || snapshot.mode === "idle" || now === 0) return <div className={`stage-empty stage-${variant}`}>Aguardando o proximo bloco</div>; const remaining = remainingSeconds({ durationSeconds: block.durationSeconds, mode: snapshot.mode, now, pausedElapsedSeconds: snapshot.pausedElapsedSeconds, startedAt: snapshot.startedAt }); return <div className={`stage-presentation stage-${variant} ${remaining < 0 ? "is-late" : ""}`}><span>{block.title}</span><strong>{formatStageTime(remaining)}</strong><small>{snapshot.mode === "paused" ? "PAUSADO" : remaining < 0 ? "TEMPO EXCEDIDO" : "TEMPO RESTANTE"}</small></div>; }
