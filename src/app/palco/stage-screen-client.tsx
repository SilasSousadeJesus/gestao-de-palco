"use client";
import { useEffect, useState } from "react";
import { StagePresentation } from "@/features/stage/stage-presentation";
import { useStageSnapshot } from "@/features/stage/use-stage-snapshot";
type EventData = { blocks: { id: string; title: string; durationSeconds: number }[] };
export function StageScreenClient({ eventId }: { eventId: string | null }) { const [selectedEventId, setSelectedEventId] = useState(eventId); const [event, setEvent] = useState<EventData | null>(null); useEffect(() => { if (eventId) return; const stored = window.localStorage.getItem("gestao-de-palco.evento-ativo"); if (stored) setTimeout(() => setSelectedEventId(stored), 0); }, [eventId]); const { snapshot } = useStageSnapshot(selectedEventId); useEffect(() => { if (!selectedEventId) return; let cancelled = false; void fetch(`/api/events/${selectedEventId}`).then((response) => response.json()).then((data: EventData) => { if (!cancelled) setEvent(data); }); return () => { cancelled = true; }; }, [selectedEventId]); const block = event?.blocks.find((item) => item.id === snapshot?.activeBlockId); return <main className="stage-screen"><StagePresentation block={block} snapshot={snapshot} variant="stage" /></main>; }
